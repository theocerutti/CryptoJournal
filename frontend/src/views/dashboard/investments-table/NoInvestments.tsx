import React from 'react';
import { Alert } from '@chakra-ui/react';

const NoInvestments = () => {
  return <Alert status='warning'>You don't have any investments yet. Add one to get started.</Alert>;
};

export default NoInvestments;
